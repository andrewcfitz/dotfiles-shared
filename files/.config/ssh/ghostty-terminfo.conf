# Ensure remote hosts have xterm-ghostty terminfo before ssh hands off to the
# shell. The exec hook runs locally on every connect, but the script caches
# per-host so the actual push only happens once. Always exits non-zero so the
# Match clause never applies — only the side effect matters.
Match host * exec "~/workspace/dotfiles/shared/bin/ghostty-push-terminfo %n"
