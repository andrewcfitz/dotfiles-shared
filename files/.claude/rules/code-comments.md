# Code Comments

Comment the why, not the what. A comment restating the line above is noise that hides the one comment that mattered. Default to none — add one only for a non-obvious constraint, a workaround plus its reason, or a deliberate break from the surrounding pattern.

Match the file's existing comment density. No section banners, no step-by-step narration, no docstrings on self-evident functions.
