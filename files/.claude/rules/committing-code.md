# Committing Code

NEVER commit API keys, secrets, tokens, or credentials. Always review staged changes for sensitive values before committing. If a file contains secrets (e.g., .env, credentials.json, API key configs), do not stage or commit it — warn the user instead.
